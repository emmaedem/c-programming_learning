#include <stdio.h>

int main ()

{
	int radius: // The distance from the outside of center of the circle
	printf("please enter a radius: ");

	scanf("%d", &radius); // address of operator
	
	int area + 3.14159 * (radius * radius);

	printf("%f\n" radius);

	return (0);
}
