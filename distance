#include <stdio.h>

int main(void)
{

	int speed = 0;
	int time = 0;

	printf('speed: ");
	scanf("%d", &speed); 

	printf('time: ");
        scanf("%d", &time);

	int distance = speed * time:
	printf(distance: %d\n", distance);

	return (0);
}i
